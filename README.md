# My custom linux installation scripts

## Usage
* Clone the repo, run `chmod a+x *.sh && chmod a+x mtrack/*.sh`
* Then run `./install-apt.sh` if you are using a distro that has the aptitude package manager.
